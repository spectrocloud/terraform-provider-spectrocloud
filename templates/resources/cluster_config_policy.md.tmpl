---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: "Cluster Policy"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}

## Example Usage

{{ tffile "examples/Cluster Configuration/Cluster Policy/resource/resource.tf" }}

Using `terraform import`, import the cluster config policy using the `cluster_config_policy_name` or `id` colon separated with `context`. For example:

```console
terraform import {{ .Name }}.{cluster_config_policy_uid}/{cluster_config_policy_name}:{project}/{tenant}
```

{{ .SchemaMarkdown | trimspace }}

