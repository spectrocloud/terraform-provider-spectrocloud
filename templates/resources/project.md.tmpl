---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: "Projects"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}

## Example Usage

{{ tffile "examples/Projects/resource/resource.tf" }}

Using `terraform import`, import the cloud account using the `project_name` or  `id` colon separated with `context`. For example:

```console
terraform import {{ .Name }}.{project_uid}/{project_name}
```


{{ .SchemaMarkdown | trimspace }}