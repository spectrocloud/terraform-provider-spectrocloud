on:
  push:
  workflow_dispatch:

jobs:
  build-with-coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/common-steps
      - name: Run tests with coverage
        run: go test -cover

