# Coming soon

This feature is not yet supported by the `terraform-provider-spectrocloud` provider - there is
no corresponding resource or data source in the provider today. This placeholder exists so the
navigation structure mirrors the full Palette product surface; it will be filled in once support
is added.

